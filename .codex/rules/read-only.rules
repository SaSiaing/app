# 네트워크가 필요한 GitHub 읽기 전용 명령만 샌드박스 밖에서 승인 없이 허용합니다.
prefix_rule(pattern = ["gh", "pr", ["view", "list"]], decision = "allow")
prefix_rule(pattern = ["gh", "issue", ["view", "list"]], decision = "allow")
